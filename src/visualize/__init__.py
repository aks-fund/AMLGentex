# Only import core plotting functionality
from src.visualize.plot import plot_metrics

# Note: features.py and data.py contain standalone scripts with additional dependencies (umap)
# Import them explicitly if needed: from src.visualize.features import plot_umap